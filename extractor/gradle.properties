# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx3G
# Fabric Properties
# check these on https://fabricmc.net/develop

minecraft_version=26.1
# NOTE: Minecraft 26.1+ is unobfuscated, so Yarn mappings no longer exist.
# Loom uses Mojang's official names directly (no `mappings` dependency).
loader_version=0.19.5
loom_version=1.17.20
# Mod Properties
mod_version=1.0.0
maven_group=dev.00a
archives_base_name=chunkedge-extractor
# Dependencies
fabric_version=0.145.1+26.1
